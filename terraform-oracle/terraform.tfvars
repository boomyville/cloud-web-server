# Authentication
# Create the ssh key by using ssh-keygen in the Oracle cloud shell
# This will then create a pub file in user/.ssh/ folder
# Copy that link
tenancy_ocid = ""
ssh_public_key = "/home/somewhere/.ssh/id_rsa.pub"

# Compartment
compartment_ocid = ""
